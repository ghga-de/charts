deployment:
  enabled: true

cronSchedule: "0 * * * *"

cronjobs:
  cleanup:
    enabled: true
    schedule: "0 3 * * *"
    successfulJobsHistoryLimit: 2
    executable: cleanup-tool
    executableArgs:
      - "--dry-run"
  report:
    enabled: true
  skipped:
    enabled: false
