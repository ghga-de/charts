image:
  repository: busybox
  tag: latest

configPrefix: ""

cronSchedule: "0 0 1 1 0"

config:
  foo: bar

cronjob:
  enabled: true

cmd: sleep 100000000

vaultAgent:
  enabled: true
  secrets:
    mongodb:
      enabled: true
      parameterName: TEST_DB
    service:
      enabled: true
